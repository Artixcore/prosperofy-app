# Public base URL of prosperofy-laravel-core (no trailing slash). Browser calls this origin only — never AI or wallet.
# Local: match laravel-core APP_URL (e.g. http://127.0.0.1:8080). Production: https://backend.prosperofy.com
NEXT_PUBLIC_LARAVEL_API_BASE_URL=http://127.0.0.1:8080

# Optional canonical URL for this app (links, redirects).
# NEXT_PUBLIC_APP_URL=https://app.prosperofy.com
